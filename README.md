## iCLP Guidance


> Current code under the `iclp/old` directory is the old version of the code. Besides, all related examples (`examples/Old`) and the corresponding configuration files (`configs/old`). Thus, please always use the old version of the code for the reproduction. The new version based on *langgraph* and *agentlighting* is under-preparation.

To run the code for the reproduction, please follow the steps below:

1. `pip install .`

2. Please check the `README.md` under the `examples/Old/LatentPlan` for how to run the code.
